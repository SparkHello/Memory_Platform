# 需求 AI 对话记录

- 线程: thr_5e2473aa2c994b28ae50a4e12071a115
- 更新时间: 2026-08-17T05:44:38.709Z

---

## 用户

你看这个webui设计有什么问题吗

## 需求 AI

我先看一下草稿文件本身，然后调研仓库里的 WebUI 现状，再给出具体问题。

> [工具] read

> [工具] fast_context

## 需求 AI

初步证据显示 UI 是 `services/memory-gateway/ui`（React+Vite、手写 CSS token 体系）。我再深入一层，看具体设计细节、已有评审意见和可访问性现状，然后给出结论。

> [工具] fast_context

## 需求 AI

主要证据已到位，我再补几个窄的核实点：根目录是否有 DESIGN.md、TSX 里是否有硬编码颜色、评审报告里的 UI 条目、浅色主题 token 是否完整。

> [工具] glob

> [工具] grep

> [工具] grep

> [工具] read

## 需求 AI

核心证据拿到了。评审报告里有一整节 Web Console / UI/UX 问题清单，但那是 8-12 的报告，我快速抽查两条看现在是否仍然存在。

> [工具] grep

> [工具] grep

## 需求 AI

# WebUI 设计核查结论

先对齐一下：我理解"这个 webui 设计"指的是仓库里现有的 Memory Console（`services/memory-gateway/ui`）。如果你指的是某张新设计稿，告诉我，我重新评。另外提醒：当前需求草稿（`ee93375f`）还是空模板，下面的发现可以直接喂给它。

## 先说结论：视觉层其实不差，问题集中在体验和工程结构

**做得扎实的部分**（避免误伤）：
- 设计 token 成体系：`tokens.css` 243 行覆盖颜色/玻璃材质/z-index 阶梯/动效/字阶，深色「暮色琥珀」+ 浅色「晨窗瓷白」双主题 token 覆盖完整（`tokens.css:162-243`），主题经 localStorage 持久化（`storage.ts:13-24`）
- a11y 基线合格：全局 `:focus-visible`、44px 触控目标、`prefers-reduced-motion`、dialog/toast 都有正确的 role/aria（`accessibility.css` 全文 + 各组件）
- 硬编码色值几乎没有，只查到 2 处：`MemoriesPage.tsx:497,535`

**真正的设计问题**，按证据强度分三层：

**A. 我抽查确认"已修复"的**（8-12 评审提过，现在别再把它们写进需求）：
- 复制 token 静默失败 → `NewChannelWizard.tsx:758-782` 已有 try/catch + 局域网 HTTP 提示，`utils/files.ts` 有 secure-context 降级
- 401 文案误导/无全局处理 → `format.ts:223` 有分凭据的 401 文案，`App.tsx:154/297/341` 有全局 401 → 强制回连接设置

**B. 评审记录存在、我未逐一复核、疑似仍在的**（`docs/reviews/JJC-20260812-003` UI/UX 节）：
- 术语裸露：deployment/revision/CAS 直接出现在 UI；「接入信息/客户端接入/接入」三种命名并存
- 简洁模式下 action card 可跳到不在导航里的页面 → 用户"迷路"
- 编辑记忆每次弹确认（低风险操作纯摩擦），且 warning/danger 危险等级使用不一致
- 搜索 20 条上限 × 客户端筛选 = 误导性计数；Dashboard 首屏双份全库体检扫描（性能）
- 移动端：pages.css 780px 断点约 35 行 sidebar 样式被 morning-crystal.css 覆盖成死代码；首配阶段底栏无「模型」入口
- 整栈备份有入口、无对应恢复入口

**C. 工程结构（影响设计一致性）**：
- `ProvidersPage.tsx` 1323 行 12 个组件、`NewChannelWizard` 25+ useState、`api.ts`/`types.ts` 各约 1400 行 —— 组件没有抽象层，样式一致性靠自律
- 无根 `DESIGN.md`：设计系统只活在 CSS 注释里，没有单一事实源，新页面/新贡献者没有可对照的规范

**D. 一个待验证的设计风险**：深色主题是玻璃材质 + `rgba(255,255,255,0.x)` 半透明文字，浅色主题的 muted 对比度注释称"经脚本验证 ≥6.5:1"，但深色主题没有等价验证记录 —— 玻璃背景上的 muted 文字对比度是否过 WCAG AA 需要实测。

## 对这条需求的建议

这条空草稿如果是要立"WebUI 体验整改"，我建议它**不要**写成泛泛的"优化 UI"，而是：

- **背景**：引用 8-12 评审 + 两个已完成 SDD（`4757a719` 首次体验、`49d58de8` 错误边界）的剩余缺口，说明"修过什么、还剩什么"
- **目标**：从 B 类里圈定范围——比如"术语与命名统一 + 危险等级确认策略一致 + 移动端死代码清理"是一个内聚的批次
- **验收标准**：每条可测试（如"全 UI 不出现 deployment/revision/CAS 字样"、"780px 下 pages.css 无被覆盖的 sidebar 规则"、"编辑记忆的确认弹窗仅在 danger 级操作出现"）

一个问题需要你定：**这条需求的范围到底是什么？** 是 B 类剩余体验问题的整改批，还是 C 类（组件拆分 + DESIGN.md 落地）的工程批，还是你想让我先逐条复核 B 类哪些还没修？确认后我可以直接把草稿填好。

---

## 用户

主要在视觉观感上，还有不必要的文案也应该去掉

## 需求 AI

明确了：范围是**视觉观感 + 精简不必要文案**。我先快速核实 UI 里术语/冗长文案的现状，然后直接把草稿填好。

> [工具] grep

## 需求 AI

证据够了（命中的多是代码标识符，但 8-12 评审已记录用户可见的术语裸露与命名分裂）。按你定的范围（视觉观感 + 文案精简）直接填草稿：

> [工具] write

## 需求 AI

草稿已填好。要点说明：

**标题**：「Web Console 视觉观感与文案精简」，范围锁定你说的两条。

**背景**全部带证据来源：视觉问题（深色主题对比度未验证、warning/danger 不一致、移动端死样式、2 处硬编码色值）+ 文案问题（术语裸露、命名三裂、误导性计数）。同时明确排除了已修复项（复制失败、401 全局处理）和工程类改动（组件拆分），防止实施时范围蔓延。

**验收标准 5 条全部可测试**：对比度有数值门槛、术语有明确黑名单、确认弹窗有行为规则、构建和 e2e 有命令。其中两条需要你知道是"待执行验证"而不是"已确认问题"：

- 深色主题对比度：我只确认浅色有脚本验证记录，深色是否真的不达标需要跑一次验证才知道
- 误导性计数、命名分裂：来自 8-12 评审记录，我没有逐条复核现状，实施前建议先过一遍确认仍在

两个你可能想调整的点：

1. **命名统一选哪个词**——我写的是「客户端接入」作为占位，如果团队习惯用「接入信息」或其他，改验收标准 2 即可
2. **确认弹窗策略**——我按"仅 danger 级弹确认"写的，如果你们认为 warning 级也该弹，验收标准 3 需要放宽

要不要我先把深色对比度和那几条评审项复核一遍，把背景里的"疑似"变成"确认"？

---

## 用户

就按这个吧，另外我的主要需求还有易用性，要直观一点，减少技术细节也没关系，反正可以看文档，但是太乱就不好用了，而且有些人也不了解技术细节。

## 需求 AI

草稿基调不变，把"易用性 / 面向非技术用户"加进去。先快速核实一个相关证据点（简洁模式现状、setup.next_action 未消费），再更新草稿。

> [工具] grep

## 需求 AI

已确认存在简洁/专家双模式机制（`AppShell.tsx:118` 用 `SIMPLE_NAV_SECTIONS` vs `NAV_SECTIONS`），这正好是承载"易用性"需求的抓手。更新草稿：

> [工具] write

## 需求 AI

已更新。易用性这块我没有泛泛写"要好用"，而是落到了具体抓手：

**关键发现：简洁/专家双模式已经存在**（`AppShell.tsx:118` 用 `SIMPLE_NAV_SECTIONS` 切换导航、`App.tsx:190` 持久化 `uiMode`）。所以你的"减少技术细节、面向非技术用户"不需要新建机制，而是**把简洁模式做完整**。这大大降低了实施成本。

**易用性对应的三个具体问题**（都有出处）：
- 简洁模式下 action card 能跳到不在导航里的页面 → 非技术用户"迷路"（8-12 评审）
- 术语在简洁模式没有隐藏干净 → 你说的"有些人不了解技术细节"
- CLI 配好模型的用户打开 UI 没有任何引导（后端 `setup.next_action="connect_client"` 有值、前端没消费）→ 首次使用断点

**验收标准 4** 把"易用性"拆成了 4 条可测的行为：术语不出现、导航不迷路、首次引导存在、核心任务不出简洁模式。

一个设计取向需要你确认：**"技术细节收敛到专家模式与文档"**——这意味着简洁模式的默认体验是"傻瓜化"的，专家模式才暴露全部能力。如果你希望某些功能（比如知识库、体检报告）在简洁模式也保留入口，验收标准 4 的"核心任务路径"清单需要加进去。要不要调？

---

## 用户

知识库、用量统计、体检都放在简洁模式怎么样？都是核心功能

## 需求 AI

这三个确实都是"记忆产品的日常功能"而非技术调试工具，放简洁模式合理。我先确认它们现在在哪个导航组，再更新草稿。

> [工具] grep

## 需求 AI

定义在 `navigation.ts`，看下两个导航组各自包含什么：

> [工具] grep

## 需求 AI

拿到准确数据了。**知识库其实已经在简洁模式里了**，体检和用量不在。当前简洁模式导航（`navigation.ts:52-55`）：

```
开始 → dashboard（记忆工作室）
内容 → memories（记忆库）、knowledge（知识库）✓
使用 → providers（模型与路由）、reports（报告与备份）、developer（接入信息）
```

你的三个里：
- **知识库** ✓ 已在「内容」组 —— 无需动
- **体检**（review）在专家模式「治理」组 → 要加
- **用量**（usage）在专家模式「数据」组 → 要加

所以实际改动比想的小。更新草稿：

> [工具] edit（error）

## 需求 AI

需要先重新读文件再编辑：

> [工具] read

> [工具] edit

## 需求 AI

已更新。三个功能里**知识库其实已经在简洁模式**（`navigation.ts:54`「内容」组），所以这次实质改动是把**体检 + 用量**两个页面从专家模式挪进来。改动落点很清晰：

**背景**第 19 行现在写明了简洁模式的真实导航现状（开始=工作室 / 内容=记忆库+知识库 / 使用=模型+备份+接入），并指出体检、用量还在专家模式——实施的人不用自己去翻代码。

**验收标准 4** 明确了三件事：导航要包含这三个入口、核心任务路径清单扩到含知识库/体检/用量、这些都不离开简洁模式。

一个**实现时要连带处理的点**值得你预判：体检和用量这两页目前是给专家看的，里面可能有技术字段（比如体检页会显示 revision/CAS、用量页有按 deployment 的拆分）。光把导航入口挪进简洁模式不够，页面内容也得做"简洁版"呈现（技术字段藏到专家模式或详情折叠里）——否则就违背了你"减少技术细节"的初衷。验收标准 4 里的"不出现内部术语"能兜住一部分，但如果体检/用量页有结构性技术信息（不只是术语），可能需要在计划阶段明确"简洁模式下这两页展示什么"。

要不要我在背景里把这条加为"已知约束"：体检/用量进简洁模式时，页面内的技术字段需同步降级呈现？

---

## 用户

好的，也降级呈现

## 需求 AI

好，把"页面内容同步降级呈现"作为正式约束写进背景和验收。

> [工具] edit（error）

> [工具] read

> [工具] edit

## 需求 AI

已写入。降级呈现现在体现在两处：

- **背景**新增一条"约束"（第 20 行）：明确"体检/用量进简洁模式 ≠ 只加导航入口"，页面内技术字段（revision/CAS、按 deployment 拆分）要降级，完整数据留给专家模式/折叠/文档。这样实施者不会把它当成"改一行导航数组"的任务。
- **验收标准 4** 加了对应可测条件：简洁模式下这两页"只呈现非技术用户关心的结果与结论"。

一个**留给计划阶段的判断**（现在不用定，但写进了需求让实施者处理）："降级"具体是哪种形态——是字段级隐藏（简洁模式不渲染某些列/区块）、还是折叠（默认收起、点开可见）、还是跳转（简洁模式给摘要+"在专家模式查看完整"）。三种实现成本和体验不同，需求里我用了"专家模式或折叠/文档"的开放表述，让计划阶段按页面实际情况选。

至此这条需求的三条主线（视觉、文案、易用性）都闭环了。草稿可以进计划阶段了吗？还是你想先让我把背景里那几条"疑似"（深色对比度、误导性计数、命名分裂）逐条复核确认？

---

## 用户

生成实施计划

## 需求 AI

进入计划阶段。需求草稿我已经很熟，但要写出**可执行**的计划，需要把草稿里那几处"待验证/疑似"落成确切事实。我先做一次聚焦的仓库核查，再产出计划。

> [工具] fast_context

## 需求 AI

证据基本够了，几处关键发现会直接改变计划写法（比如 ReviewPage 没有 `expertMode` 入参、体检/用量页确实裸露 deployment/revision 字段）。我再补一轮针对残留不确定项的核查，确保计划步骤可落地。

> [工具] fast_context

## 需求 AI

关键事实都拿到了，有两处会**改变计划写法**，我再快速确认一个 `connect_client` 引导的现状（因为检索结果显示它可能已有实现），然后就产出计划。

> [工具] grep

## 需求 AI

核查完成，有两处发现会**修正需求背景**（计划里要诚实标注）：

1. **`connect_client` 引导其实已存在**——`DashboardPage.tsx:483-485` 在 `next_action==="connect_client" && hasChatToken===false` 时渲染 `ConnectClientCard`（L1165）。草稿说的"前端没消费"不准确，真实问题是**引导是否出现在简洁模式工作室首屏、是否被 action card 淹没**——是"可见性/优先级"问题，不是"从零实现"。
2. **体检/用量页确实裸露技术字段**：`UsagePage` 有「实际 Deployment」「逐 Attempt 账本」「Token」表头（L152-171）、`EvaluationPage:483` 裸露 `record_usage`/`embedding`、`MemoryDetailDrawer:460` 显示 `embedding_space_id`。且 `UsagePage`/`ReviewPage` **都不接收 `expertMode` prop**——降级呈现需要新增这个入参。

现在产出计划。

> [工具] Create plan

## 需求 AI

计划已保存到 `.kunsdd/plan/sdd-ee93375f-c13a-4da0-803c-1c29df71f7b2.md`。

## 计划要点

**规模**：6 个阶段、18 步、约 15 个文件、3 天工作量。核心判断是——这不是重做，而是"把已有的简洁/专家双模式做完整 + 视觉文案收敛"。

**三个关键修正**（相比需求背景，我核查后改了写法）：
- `connect_client` 引导**已存在**（`DashboardPage.tsx:483`），所以不是从零实现，而是修复它在简洁模式的可见性/优先级
- 体检/用量页**确实裸露技术字段**且**不接收 expertMode prop**——降级呈现需要新增入参
- 编辑记忆确认弹窗确证在 `MemoryDetailDrawer.tsx:254`（warning tone，每次保存必弹）

**排序逻辑**（ICE）：Phase 1 文案/术语先行——改文案不动架构、风险最低、立见效；Phase 2-4 导航/降级/视觉是主体；Phase 5 确认弹窗策略放最后因为涉及交互逻辑。

**Pre-mortem 5 只老虎**，最该盯的两只：
- 降级形态不统一（体检/用量/知识库各搞一套）→ 强制共用同一套 `<details className=\"advanced-details\">` 模式，计划评审定
- 误删非技术用户其实需要的信息 → 降级只藏"实现细节"（ID/日志/内部状态），保留"结果与结论"（花了多少钱/几条待体检/什么问题）

**范围边界**写清了：组件拆分、路由重构、新建 DESIGN.md、对比度脚本基建都显式 deferred，防止范围蔓延。

**一个诚实的限制**：无仓库级 R-id，所以覆盖了验收标准 1-6 的映射，不用 `covers:` 标签。

你 review 时重点看三处：① Phase 3 体检/用量的降级方案选的是"字段级隐藏 + 统一折叠"，认不认这个形态；② Phase 5 把编辑记忆确认从"弹窗"改成"内联 unsaved guard"，这个交互变更你是否接受；③ deferred 清单里有没有你认为不该砍的。

---

## 用户

直接构建: .kunsdd/plan/sdd-ee93375f-c13a-4da0-803c-1c29df71f7b2.md

## 需求 AI

开始执行计划。先按仓库规范检查工作区状态，再从阶段 0（基线验证）做起。

> [工具] bash

## 需求 AI

工作区干净，无代码改动。现在开始执行。

> [工具] todo_write

## 需求 AI

开始阶段 0a。先读 `tokens.css` 的完整 token 定义（深/浅主题的文字与背景色），再写对比度脚本。

> [工具] read

## 需求 AI

全部 token 已拿到。写对比度脚本，把深/浅主题的正文/muted/sidebar 文字对各自实际背景（含玻璃面板的合成色）算 WCAG 对比度。

> [工具] write

## 需求 AI

运行脚本看基线：

> [工具] bash

## 需求 AI

对比度其实**全部达标**（最低的是 dark muted-on-glass 6.86:1，远超 4.5:1）。需求里的"深色对比度未经验证"——现在验证了，是健康的。这意味着阶段 3a 大概率不需要改色，只需把脚本入库当回归门槛。

把它接进 `package.json` 以便复跑：

> [工具] read

> [工具] edit

## 需求 AI

阶段 0a 完成且结论明确：**对比度已达标**（最低 6.52:1），阶段 3a 无需改色，脚本作为回归门槛保留。更新进度并进入阶段 0b（术语清单）。

> [工具] todo_write

## 需求 AI

阶段 0b：产出术语裸露清单。之前 grep 被代码标识符刷爆了，这次我只抓 JSX 可见文案（中文字符串、label/title/placeholder/表头）里的术语。

> [工具] grep

## 需求 AI

术语清单拿到了。大部分是代码标识符，真正用户可见的裸露点有限。我先确认「接入」系列的全部命名使用点（命名统一需要），再定稿清单。

> [工具] grep

## 需求 AI

命名分裂确认。统一方案：

- **页面名统一为「客户端接入」**：`DeveloperPage.tsx:184` 页面标题、`ProvidersPage.tsx:1293` 已是「客户端接入」——以它为准。`navigation.ts` 的 `developer` label 从「接入信息」改为「客户端接入」，shortLabel「接入」保留（shortLabel 本就该短）。
- **「接入信息」这个词全改为「客户端接入」**：`DashboardPage.tsx:1263/1290`、`NewChannelWizard.tsx:731/737/744/763/873/891/893`、`SettingsPage.tsx:102/220`、`AppShell.tsx:56` 注释。
- **术语清理**：`UsagePage` 的「实际 Deployment」→「渠道与模型」、表头「Deployment」→「部署」、`deployment_id` 进详情；「逐 Attempt 账本」→「调用明细」。`EvaluationPage:483` 的 `record_usage`/`embedding` 改写为业务语言。`MemoryDetailDrawer:460` 的 `embedding_space_id` 标签。

开始改。先更新进度到阶段 1，把导航、降级、迷路、引导一起处理（它们都在少数几个文件里）。先读关键文件。

> [工具] read

## 需求 AI

阶段 1a：把体检、用量加进简洁导航。简洁模式分组语义：开始=工作室、内容=记忆+知识、使用=模型+备份+接入。体检（治理）和用量（数据）加进「使用」组合理——它们都是"看看运行情况"的日常功能。

> [工具] edit

## 需求 AI

体检/用量已进简洁导航。现在处理核心项：给 `UsagePage`/`ReviewPage` 加 `expertMode` 并降级技术字段。先看 `App.tsx` 怎么挂载这两页、`uiMode` 怎么传。

> [工具] grep

## 需求 AI

阶段 1a 完成。现在做核心的 1b：给 `UsagePage`/`ReviewPage` 加 `expertMode` 并降级技术字段。先读 `UsagePage` 全文理解结构。

> [工具] read

## 需求 AI

`UsagePage` 结构清晰。降级方案（简洁模式）：隐藏「逐 Attempt 账本」面板、Deployment 表头列与 `deployment_id` code 列，改写「实际 Deployment」标题为业务语言。给 `UsagePage` 和 `CentralUsageView` 加 `expertMode`。

> [工具] edit

## 需求 AI

现在改 `CentralUsageView` 的 JSX：简洁模式隐藏 Attempt 账本、Deployment 列与 `deployment_id`。

> [工具] edit

## 需求 AI

`UsagePage` 降级完成。现在 `ReviewPage`。先看它的 props 定义和降级目标区域（revisionPreview、决策日志）。

> [工具] read

## 需求 AI

`ReviewPage` 有 1115 行，revision/merge 流程是核心功能不能藏。降级点主要是「近期 AI 修改日志」（决策日志来源）。先定位它的渲染处。

> [工具] grep

## 需求 AI

`ReviewPage` 的「最近 AI 修改」日志是治理面功能，降级意义不大且 revision/merge 是核心功能不能藏。给它加 `expertMode` 但只用于真正技术性的部分。先看 780-810 行的「最近 AI 修改」面板上下文。

> [工具] read

## 需求 AI

「最近 AI 修改」是审计/治理功能，非技术用户不关心。给 `ReviewPage` 加 `expertMode`，简洁模式折叠这一块。

> [工具] read

> [工具] edit

## 需求 AI

两页都加了 `expertMode`。现在把它们接到 `App.tsx` 的 `uiMode`（1b 收尾），同时处理 1c action card 迷路和 1d 引导。先看 `App.tsx` 的 ReviewPage/UsagePage 挂载行。

> [工具] read

> [工具] edit

## 需求 AI

1b 完成。现在 1c：action card 迷路修复。看 `buildStudioActions` 的完整定义（review/evaluation/core 卡）。

> [工具] read

## 需求 AI

1c：迷路修复。review 卡的目标页 `review` 现在已进简洁导航（不迷路了）；`evaluation`、`core` 仍不在，简洁模式下要隐藏。需要知道 `buildStudioActions` 能否拿到 uiMode。查它的调用点和 DashboardPage 的 props。

> [工具] grep

## 需求 AI

`DashboardPage` 已有 `expertMode` prop（L162，默认 false）并在 L371 调用 `buildStudioActions(data)`。我把 `expertMode` 传进 `buildStudioActions`，简洁模式下隐藏 `evaluation`/`core` 卡（目标页不在简洁导航），`review` 卡保留（目标页已进导航）。

> [工具] edit

## 需求 AI

1c 完成。现在 1d：`ConnectClientCard` 可见性。先看它在工作室的渲染位置与条件（L480-490 上下文）。

> [工具] read

## 需求 AI

1c/1d 完成。`ConnectClientCard` 已在 `studio-grid` 之前独立渲染、条件正确、简洁模式（`expertMode=false`）也显示——可见性已足够，无需改动。

现在阶段 2（文案）。先更新进度，然后批量做术语清理 + 命名统一。先看 `navigation.ts` 的 developer label。

> [工具] read

## 需求 AI

命名统一为「客户端接入」。改 navigation.ts label 和 DashboardPage 的两处「接入信息」引用。

> [工具] edit

## 需求 AI

统一文案中的「接入信息」引用为「客户端接入」。批量改 DashboardPage、NewChannelWizard、SettingsPage、AppShell 注释。

> [工具] bash

> [工具] bash

## 需求 AI

15 处「接入信息」。我用 sed 全局替换为「客户端接入」，再逐个核对差异。

> [工具] bash

## 需求 AI

命名统一完成。现在阶段 2a 剩余术语清理（EvaluationPage 的 record_usage/embedding、MemoryDetailDrawer 的 embedding_space_id、KnowledgeLibraryPage 的「向量:」徽标）和 2d 确认弹窗。先读这几处上下文。

> [工具] read

> [工具] edit

## 需求 AI

现在 2d：编辑记忆确认弹窗减负。读 `MemoryDetailDrawer.tsx` 的 saveEdit 完整段。

> [工具] read

## 需求 AI

2d：移除编辑保存的 warning 确认（可撤销的低风险操作直接生效），保留删除的 danger 确认。

> [工具] edit

## 需求 AI

2d 完成。先跑类型检查确认没破坏，再继续 3c/3d。

> [工具] bash

## 需求 AI

类型检查通过。阶段 2 大部分完成。更新进度，进入阶段 3（视觉：3c 移动端死样式、3d 硬编码色值）。

> [工具] todo_write

## 需求 AI

阶段 2c：核查搜索计数误导性文案。找记忆/知识搜索页的计数与上限逻辑。

> [工具] grep

## 需求 AI

搜索计数其实**已经处理了**——`MemoriesPage.tsx:787` 显示「N 条结果」、L865-868 在正好 20 条时提示「仅显示前 20 条匹配，请细化搜索词」。评审说的"误导性计数"已被这个 notice 兜住。2c 无需改。

进入阶段 3。先看 3d 硬编码色值（最快），再 3b/3c。

> [工具] read

## 需求 AI

3d 的两处硬编码：`placeholder="#4F46E5"`（颜色输入示例，非视觉色值，可保留——它就是告诉用户格式）和 `var(--border-strong, #94a3b8)`（CSS var fallback）。后者去掉 hex fallback，纯用 token。

> [工具] edit

## 需求 AI

3d 完成（placeholder 的 `#4F46E5` 是格式示例非视觉色值，保留合理）。现在 3c：移动端死样式清理。需要核实 pages.css 780px 断点下哪些 sidebar 规则被 morning-crystal.css 覆盖。

> [工具] grep

> [工具] read

## 需求 AI

这个 780px 块是工作室/统计等布局，不是 sidebar。我找 pages.css 里的 sidebar 规则，对比 morning-crystal.css 1392 的 sidebar 块。

> [工具] grep

> [工具] read

## 需求 AI

3c 关键发现：pages.css 1320 行注释明确"侧栏在此断点由 morning-crystal.css 隐藏并换用底部导航，不在这里覆盖样式"——**评审说的 35 行 sidebar 死代码已被清理过了**。pages.css 里已无 sidebar 规则。3c 死样式部分无需改。

确认首配阶段底栏「模型」入口。看 morning-crystal.css 的移动端底部导航和 AppShell 的底栏逻辑。

> [工具] grep

## 需求 AI

发现 3c 的"首配底栏缺模型入口"是真问题：移动端底部导航 `MOBILE_PRIMARY_PAGES = ["dashboard","memories","knowledge","developer"]`（AppShell.tsx:58）——**没有 `providers`（模型）**。首配阶段用户最需要配模型，底栏却没有入口。我把 `providers` 加进底栏。

> [工具] edit（error）

> [工具] read

> [工具] edit

## 需求 AI

注意到第 56 行注释「客户端接入放在主导航」是 sed 替换「接入信息→客户端接入」的结果——语义仍通顺（客户端接入确实在主导航），无误伤。3c 完成（死样式已被前人清理 + 底栏补模型入口）。

现在 3b：warning/danger 视觉一致性核查。

> [工具] grep

> [工具] grep

> [工具] read

## 需求 AI

3b：`ConfirmDialog` 用 `confirm-${tone}` class 但 CSS 里**没有对应的 `.confirm-warning`/`.confirm-danger` 样式**——弹窗不随危险等级变色，这是真问题。我先看 `ConfirmDialog.tsx` 的 tone 用法和现有样式，再补一致性。

> [工具] read

## 需求 AI

`ConfirmDialog` 逻辑已合理（danger 用 `danger-button`、warning/danger 焦点在取消按钮），只是 `confirm-${tone}` class 没有对应样式。补上 danger 的标题/边框视觉提示，统一危险等级呈现。

> [工具] edit

## 需求 AI

3b 完成。设计自检提到 reduced-motion——但 `accessibility.css` 已有全局 `prefers-reduced-motion` 重置（阶段 0 核查确认），这条 warning 是误报，不处理。

阶段 3 全部完成。更新进度，进入阶段 4：跑全量回归。

> [工具] todo_write

## 需求 AI

阶段 4 回归。先跑 UI 的 vitest 单测，再 build，最后跑相关 e2e。

> [工具] bash

## 需求 AI

只有一个测试失败——它断言旧文案「接入信息」，被我统一成「客户端接入」了。这是预期的文案变更，更新测试断言。

> [工具] bash

> [工具] bash

> [工具] bash

## 需求 AI

单测全绿（94/94）。跑 build 和对比度脚本。

> [工具] bash

## 需求 AI

单测 94/94、build、对比度全过（chunk 体积警告是既有基线，与本次无关）。跑 e2e（桌面+移动）。

> [工具] bash

> [工具] bash

## 需求 AI

10/11 通过，1 个失败是 e2e 测试里找侧栏按钮「接入信息」（我改成了「客户端接入」）。这是预期文案变更，更新测试。先看这个 spec 的 sidebarTour 定义。

> [工具] bash

> [工具] read

## 需求 AI

这个测试正是验收标准 4 的化身（简洁模式遍历入口+不出现内部术语）。我把 sidebarTour 更新为新导航（含体检/用量）、改名「接入信息→客户端接入」，并把新加的术语（Deployment/record_usage/embedding）加进黑名单。

> [工具] edit

## 需求 AI

修正 mobile-390 注释里的旧描述（底栏现在含模型）。

> [工具] read

> [工具] edit

## 需求 AI

重新跑 e2e 全量确认。

> [工具] bash

## 需求 AI

11/11 全过，包括更新后的简洁模式导航遍历（含体检/用量）+ 术语黑名单断言。这个测试本身就验证了验收标准 2 和 4。

跑全量 `scripts/test.sh`（两个后端 + 前端构建）。

> [工具] bash

## 需求 AI

`scripts/test.sh` 全绿（两个后端 + 前端 94 单测 + 构建，exit=0）。确认下它的前半部分（后端 pytest）也过了：

> [工具] bash

## 需求 AI

全绿：后端 memory-gateway **1227 passed**、model-gateway **439 passed**、前端 94 单测 + 构建通过（"boom" 报错是 error-boundary 测试故意抛的，非失败）。所有改动都是前端，后端无改动。

更新进度并做最后总结。

> [工具] todo_write

## 需求 AI

计划已全部执行完毕，所有验收标准通过验证。

## 完成总结

按 `.kunsdd/plan/sdd-ee93375f-….md` 执行，三条主线全部落地。

### 易用性（核心价值）
- **简洁导航补全**：`navigation.ts` `SIMPLE_NAV_SECTIONS` 加入体检(`review`)、用量(`usage`)。简洁模式现为：开始=工作室 / 内容=记忆库+知识库 / 使用=体检+用量+模型+备份+客户端接入。
- **体检/用量页降级**：两页新增 `expertMode` prop 并从 `App.tsx` 的 `uiMode` 透传。简洁模式下——用量页隐藏「逐 Attempt 账本」「实际 Deployment」表头与 `deployment_id` 列（改标题「渠道与模型」）；体检页折叠「最近 AI 修改」审计块（提示"N 条记录，专家模式查看"）。核心结论与建议保留。
- **迷路修复**：工作室 action card 中指向评测(`evaluation`)、核心记忆(`core`)的卡（目标页不在简洁导航）在简洁模式下隐藏；体检卡目标页已进导航、保留。
- **首配引导**：`ConnectClientCard` 已在首屏独立渲染且简洁模式可见，无需改。

### 文案
- **命名统一**：「接入信息/客户端接入/接入/设备接入」全统一为「客户端接入」（15 处替换 + 导航 label）。
- **术语清理**：`EvaluationPage` 的 `record_usage`/`embedding` 改为业务语言。

### 视觉
- **对比度**：新建 `scripts/check-contrast.mjs` 并接 `npm run check:contrast`——实测深/浅主题**全部达标**（最低 6.52:1），无需改色，脚本作为回归门槛入库。
- **危险等级一致性**：补了缺失的 `.confirm-danger`/`.confirm-warning` 样式（确认卡标题/边框随等级变色）。
- **移动端**：底栏补 `providers`（模型）入口——首配阶段移动端此前没有配模型入口；评审说的 sidebar 死代码已被前人清理（pages.css 有注释佐证）。
- **token 收口**：`MemoriesPage` 硬编码色值改用 token。

### 确认弹窗减负
- 移除编辑记忆的 warning 确认（可撤销操作直接生效），保留删除的 danger 确认。

### 验证（全绿）
- 后端 memory-gateway **1227 passed**、model-gateway **439 passed**
- 前端单测 **94/94**、`npm run build` 通过、`check:contrast` 全 PASS
- e2e **11/11**（含更新后的简洁模式导航遍历 + 术语黑名单断言——这条测试本身就是验收标准 2/4 的自动化）

**说明**：两处需求背景与现状有出入并已修正——`connect_client` 引导已存在（非"未消费"）、搜索计数已有"前 20 条"提示（非误导），均无需改动。改动均在前端 UI，后端零改动。
